services:
  cytex-texlive:
    image: aa9991/cytex-texlive:0.1.0
    # build: . # if you want to build the image locally
    volumes:
      - ../:/workspaces/CyTeX
      - ./.latexmkrc:/root/.latexmkrc
    tty: true
    environment:
      SHELL: "/bin/bash"
